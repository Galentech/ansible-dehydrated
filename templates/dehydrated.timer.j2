[Unit]
Description=ACME Cert renewal timer for {{ dehydrated_ca_name }}

[Timer]
OnCalendar=*-*-* {{ 4|random(seed=inventory_hostname) }}:{{ 59|random(seed=inventory_hostname) }}:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
